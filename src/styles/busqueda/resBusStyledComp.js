import styled from 'styled-components';
import { Container, middleTitle } from '../../globalStyle';

export const StyledBusqueda = styled(Container)`

`;

export const TitleBusqueda = styled(middleTitle)`
  margin-bottom: 25px;
`;