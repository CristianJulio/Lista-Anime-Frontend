import styled from 'styled-components';
import { Container } from '../../globalStyle';
import { middleTitle } from '../../globalStyle';

export const StyledBrowser = styled(Container)`

`;

export const TitleBrowser = styled(middleTitle)`
  margin-bottom: 25px;
`;